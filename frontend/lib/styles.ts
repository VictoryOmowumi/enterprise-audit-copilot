/**
 * Elevated panel used for every card-level surface on the canvas. Light mode
 * lifts it with a soft shadow; dark mode, where shadows vanish, uses a hairline.
 */
export const surface =
  "rounded-2xl bg-surface shadow-[0_1px_2px_rgba(20,20,20,0.04),0_12px_32px_-18px_rgba(20,20,20,0.18)] dark:shadow-none dark:ring-1 dark:ring-line"

/** Figures and telemetry: IBM Plex Sans with tabular numerals. */
export const data = "font-data tabular-nums"
